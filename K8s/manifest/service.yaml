apiVersion: v1
kind: Service
metadata:
  name: atlas-api-svc
  labels:
    app: atlas-svc
spec:
  selector:
    app: atlas-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3030
  type: ClusterIP
